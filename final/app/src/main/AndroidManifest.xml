<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.afinal">
<!--    @mipmap/ic_launcher-->
    <application
        android:allowBackup="true"
        android:icon="@drawable/che"
        android:label="@string/app_name"
        android:roundIcon="@drawable/che"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".buletoothActivity"></activity>
        <activity android:name=".ledActivity" />
        <activity android:name=".wenduActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".splashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>